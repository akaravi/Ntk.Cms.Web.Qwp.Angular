# راهنمای کامل انتقال پروژه

## ✅ کارهای انجام شده

### بخش اول: حذف وابستگی به CDN

تمام منابع خارجی از CDN حذف شده و به صورت محلی نصب شده‌اند:

1. **فونت Roboto** - از Google Fonts CDN حذف شد
   - نصب شده: `typeface-roboto` از npm
   - اضافه شده به `angular.json`

2. **Material Icons** - از Google Fonts CDN حذف شد
   - نصب شده: `material-icons` از npm
   - اضافه شده به `angular.json`

3. **Bootstrap CSS** - از unpkg CDN حذف شد
   - نصب شده: `bootstrap@3.3.7` از npm
   - اضافه شده به `angular.json`

4. **اسکریپت خارجی** - حذف شد
   - اسکریپت ads از `index.html` حذف شد
   - در صورت نیاز می‌توانید به صورت محلی اضافه کنید

### فایل‌های تغییر یافته

- ✅ `src/index.html` - تمام لینک‌های CDN حذف شد
- ✅ `angular.json` - منابع محلی اضافه شد
- ✅ `package.json` - پکیج‌های جدید اضافه شد

## 📋 برنامه ارتقا به Angular 20

برنامه کامل در فایل `MIGRATION_PLAN.md` موجود است. خلاصه مراحل:

### ⚠️ نکته مهم
**هرگز مستقیماً از Angular 15 به Angular 20 نپرید!** باید مرحله به مرحله ارتقا دهید:

1. Angular 15 → Angular 16
2. Angular 16 → Angular 17
3. Angular 17 → Angular 18
4. Angular 18 → Angular 19
5. Angular 19 → Angular 20

### مراحل ارتقا

برای هر مرحله:

```bash
# مثال: ارتقا به Angular 16
ng update @angular/core@16 @angular/cli@16
ng update @angular/material@16

# سپس تست کامل
ng test
ng build --configuration production
```

### تغییرات مهم در Angular 17+

- **Standalone Components**: از Angular 17 به بعد، توصیه می‌شود از Standalone Components استفاده کنید
- **حذف NgModules**: در نسخه‌های جدیدتر، NgModules اختیاری شده‌اند
- **TypeScript**: نیاز به نسخه جدیدتر TypeScript

## 🧪 تست آفلاین

برای اطمینان از اینکه پروژه بدون اینترنت کار می‌کند:

```bash
# 1. Build پروژه
ng build --configuration production

# 2. قطع اینترنت

# 3. اجرای پروژه
npx http-server dist/ntk-cms-web-qwp -p 4200

# 4. باز کردن در مرورگر
# http://localhost:4200

# 5. بررسی Network Tab در DevTools
# باید هیچ failed request وجود نداشته باشد
```

## 📦 پکیج‌های جدید نصب شده

```json
{
  "bootstrap": "^3.3.7",
  "typeface-roboto": "^1.1.13",
  "material-icons": "^1.13.14"
}
```

## 🔍 بررسی منابع باقی‌مانده

### منابعی که باید باقی بمانند (نیازی به تغییر نیست):

1. **API Endpoints** در `environment.ts` و `environment.prod.ts`
   - این‌ها آدرس‌های backend هستند
   - برای ارتباط با سرور نیاز دارند

2. **لینک‌های خارجی** در HTML
   - لینک‌های معمولی به وب‌سایت‌های دیگر (نه CDN)
   - مثل `http://ntk.ir` و `https://karavi.ca`

## 📝 فایل‌های مستندات

1. **`MIGRATION_PLAN.md`** - برنامه کامل ارتقا به Angular 20
2. **`CDN_REMOVAL_SUMMARY.md`** - خلاصه حذف CDN
3. **`FONT_DOWNLOAD_INSTRUCTIONS.md`** - دستورالعمل دانلود فونت‌ها (در صورت نیاز)

## ⚡ مراحل بعدی

1. ✅ تست پروژه با منابع محلی
2. ⏳ شروع ارتقا به Angular 16 (اولین مرحله)
3. ⏳ تست کامل بعد از هر مرحله ارتقا
4. ⏳ ادامه تا رسیدن به Angular 20

## 🛠️ دستورات مفید

```bash
# بررسی نسخه Angular
ng version

# بررسی وابستگی‌های قدیمی
ng update

# Build برای production
ng build --configuration production

# اجرای تست‌ها
ng test

# اجرای E2E tests
ng e2e
```

## 📞 پشتیبانی

در صورت بروز مشکل:
1. بررسی فایل `MIGRATION_PLAN.md` برای جزئیات بیشتر
2. استفاده از `ng update --help` برای راهنمایی
3. مراجعه به [Angular Update Guide](https://update.angular.io/)

---

**تاریخ:** ۲۰ ژانویه ۲۰۲۶
**وضعیت:** ✅ حذف CDN کامل شده | ⏳ ارتقا به Angular 20 در انتظار
